1. Use the files in the "replicationcode" folder to replicate the tables and figures in the draft.
2. The main Stata file is "Analysis.do". It contains the code to replicate all tables and figures in the draft.
3. The "cleansurvey.do" cleans up the survey data.
